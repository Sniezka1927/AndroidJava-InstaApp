<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nodeinstaappandroid">

    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NodeInstaAppAndroid">
        <activity
            android:name=".accountEdit"
            android:exported="true" />
        <activity
            android:name=".collectionsView"
            android:exported="true" />
        <activity
            android:name=".createView"
            android:exported="true" />
        <activity
            android:name=".searchView"
            android:exported="true" />
        <activity
            android:name=".accountView"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>